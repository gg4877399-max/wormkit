name: Build WormKit APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libffi-dev \
            libssl-dev \
            python3-pip \
            python3-dev \
            build-essential \
            git \
            openjdk-17-jdk \
            unzip \
            wget \
            autoconf \
            automake \
            libtool \
            pkg-config
          pip install --upgrade pip
          pip install buildozer cython virtualenv

      - name: Build APK with Buildozer
        run: |
          buildozer android debug

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: wormkit-apk
          path: bin/*.apk

      - name: Create Release
        uses: softprops/action-gh-release@v2
        if: github.ref == 'refs/heads/main'
        with:
          files: bin/*.apk
          tag_name: build-${{ github.run_number }}
          name: WormKit Build ${{ github.run_number }}
          body: |
            Automated build of WormKit APK
            Build number: ${{ github.run_number }}
            Commit: ${{ github.sha }}
          make_latest: true